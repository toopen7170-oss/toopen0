name: Build APK 215-FINAL-SHIELD

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Setup
        run: |
          sudo apt update
          sudo apt install -y git zip unzip autoconf libtool pkg-config zlib1g-dev \
            libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev \
            build-essential python3-dev ant lld libsqlite3-dev
          
          pip install --upgrade pip
          pip install "Cython<3.0" buildozer kivy pillow pyjnius

          # [방어] 폴더 생성 및 라이선스 강제 승인
          mkdir -p ~/.android
          touch ~/.android/repositories.cfg

          export ANDROID_HOME=$ANDROID_SDK_ROOT
          export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools
          
          # [핵심 방어] 모든 버전의 라이선스를 한 번에 승인 (yes를 무한히 보냄)
          yes | sdkmanager --sdk_root=$ANDROID_HOME --licenses || true
          
          # [핵심 방어] 33 버전 빌드 도구를 수동으로 미리 꽂아넣기
          yes | sdkmanager --sdk_root=$ANDROID_HOME "platform-tools" "build-tools;33.0.0" "platforms;android-33"

          # [방어] AIDL을 못 찾는 오류 방지를 위한 환경 변수 고정
          export BUILDOZER_ALLOW_KIVY_MASTER=1
          buildozer -v android debug
        shell: bash
