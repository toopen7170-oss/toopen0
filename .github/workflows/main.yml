name: Build APK 215-FINAL-SHIELD

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Setup and Execution
        run: |
          # 1. 시스템 의존성 설치
          sudo apt update
          sudo apt install -y git zip unzip autoconf libtool pkg-config zlib1g-dev \
            libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev \
            build-essential python3-dev ant lld libsqlite3-dev
          
          pip install --upgrade pip
          pip install "Cython<3.0" buildozer kivy pillow pyjnius

          # 2. SDK 경로 강제 매핑 (환경적 요인 방어)
          export ANDROID_HOME=$ANDROID_SDK_ROOT
          export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools
          
          # 3. 라이선스 승인 (Broken pipe 방어)
          echo "y" | sdkmanager --sdk_root=$ANDROID_HOME --licenses || true

          # 4. 빌드 실행 (NoSectionError 방지를 위해 최소한의 환경 변수만 사용)
          buildozer -v android debug
        shell: bash
